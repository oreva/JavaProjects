Please follow these steps to run the application:
1. Install MySQL DB schema by running the sql script \src\main\resources\sql\init_schema.sql
2. Compile and run the application by calling the following command:
mvn clean compile exec:java